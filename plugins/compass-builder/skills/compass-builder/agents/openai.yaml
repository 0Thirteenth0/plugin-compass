interface:
  display_name: "Compass Builder"
  short_description: "Coordinate safe resumable repository builds"
  default_prompt: "Use $compass-builder to plan the fastest safe verified build mode for this repository."
policy:
  allow_implicit_invocation: true
